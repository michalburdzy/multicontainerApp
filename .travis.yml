language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t michalburdzy/multicontainer/server-test -f ./server/Dockerfile ./server
  - docker build -t michalburdzy/multicontainer/client-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true michalburdzy/multicontainer/server-test npm run test
  - docker run -e CI=true michalburdzy/multicontainer/client-test npm run test

after_success:
  - docker build -t michalburdzy/multicontainer/client ./client
  - docker build -t michalburdzy/multicontainer/server ./server
  - docker build -t michalburdzy/multicontainer/worker ./worker
  - docker build -t michalburdzy/multicontainer/worker ./worker
  - docker build -t michalburdzy/multicontainer/nginx ./nginx

  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push michalburdzy/multicontainer/client
  - docker push michalburdzy/multicontainer/server
  - docker push michalburdzy/multicontainer/worker
  - docker push michalburdzy/multicontainer/worker
  - docker push michalburdzy/multicontainer/nginx


# deploy:
#   provider: elasticbeanstalk
#   region: "eu-central-1"
#   app: "multicontainerApp"
#   env: "MulticontainerApp-env"
#   bucket_name: ""
#   bucket_path: "multicontainerApp"
#   on:
#     branch: master
#   access_key_id: $AWS_ACCESS_KEY
#   secret_access_key: $AWS_SECRET_KEY
